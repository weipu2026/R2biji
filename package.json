{
  "name": "jmbiji",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "gen-config": "node scripts/gen-config.mjs",
    "deploy": "node scripts/gen-config.mjs && wrangler deploy --config wrangler.deploy.toml",
    "test": "node --test tests/*.test.mjs",
    "test:falsify": "node tests/falsify.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
