# 安全说明

JMbiji 是**端到端加密**的云笔记。这份文件说明它到底防住什么、防不住什么。

## 零知识:服务器看不到你的内容

| 环节 | 发生在哪里 | 服务器能看到什么 |
|---|---|---|
| 主密码输入 | 浏览器 | **什么都看不到**——主密码从不发送,只参与本地 PBKDF2 派生 |
| 内容加解密 | 浏览器 | 只有密文:文件头 + 随机 nonce + AES-GCM 密文 |
| 保存 | Worker → R2 | 密文、对象键、文件大小、时间戳 |
| 访问密钥门 | Worker | 只存 `SHA-256(密码令牌)`,不存可逆的凭据 |

密钥链路:主密码 → PBKDF2-HMAC-SHA256(100 万次)→ KEK → AES-KW 包裹随机 DEK →
HKDF 派生三把子密钥(内容 / 附件 / 文件名)+ 一把独立鉴权令牌。
即使 `vault.json` 与全部分类密文被完整拿走,拿到的也只是一堆解开需要的密文。

## 防住的攻击

- 拿到网址的人:没有访问密钥连 `vault.json` 都拿不到(`ACCESS_KEY` 未配置时服务端**整体 503**,不静默降级)。
- 离线穷举主密码:PBKDF2 100 万次 + 强制 ≥10 位且估算熵 ≥64 bit 的密码策略。
- 爆破访问密钥:连续失败封 IP 并有速率上限,且比较走定长实现(不泄露时序)。
- XSS / 注入:全站零 `innerHTML`、不解析任何 HTML、严格 CSP。
- 跨站攻击:不返回任何 CORS 头、`OPTIONS` 一律 405、写操作必须带自定义头。
- 缓存泄露:Service Worker 显式拒绝 `/api/*`,密文不会被留进 Cache Storage。
- 搜索索引:`robots.txt` + `X-Robots-Tag` 全站禁索引。

## 没防住的(请按此评估你的用法)

1. **忘记主密码 = 数据永久无法恢复。** 没有任何后门,这是零知识的代价。
2. **Cloudflare 账户被入侵。** 能改 Worker 代码的人,可以在你下次打开网页时替换前端来窃取主密码——
   这是所有云端零知识方案的共同上限,本方案也不例外。
   所以请给账户开两步验证,并保管好 API Token。
3. **R2 桶被开成公开访问。** 密文会被任何人直接下载,且访问密钥门(只拦 `/api/*`)会**被整体绕过**。
   桶必须保持私有。用仓库里的 GitHub 流水线部署时,每一步都会自动检查这一项。
4. **元数据。** 分类名出现在对象键上(有意为之,便于人工辨认与抢救),所以能读桶的人看得到分类名与文件大小,看不到内容。
5. **源代码公开。** 仓库可以公开——内容安全不依赖代码保密。但**站点入口地址不是秘密**,
   真正的门与锁是访问密钥 + 主密码。

## 报告安全问题

请用 GitHub 的私密漏洞报告(**Security** 标签页 → *Report a vulnerability*),
不要开公开 issue —— 公开细节会同时告诉所有人怎么打还没修好的版本。
我会先确认影响范围,再决定修复与公开披露的时机。
