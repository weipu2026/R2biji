# JMbiji(加密笔记本)程序设计文档

> 版本:v2(云端版,已实现)。本档是实现的唯一依据;只记录最终结论。

## 1. 项目介绍

一个**端到端加密的云笔记**,解决「API 中转站信息、令牌、教程、重要记录」的跨设备存放与随手查阅:

- **形态**:单页 Web 应用 + Cloudflare Worker 后端;数据存 R2,**服务器只存解不开的密文(零知识)**。
- **核心用法是「读」和「复制」**,不是写:打开即阅读态,字段级一键复制。
- **加密**:所有上传内容均为密文(AES-256-GCM);主密码不出浏览器。
- **零依赖**:前端纯原生 JS + Web Crypto,后端纯 Workers 运行时;无任何第三方运行时库、无构建步骤。

### 目标 / 非目标

| 目标 | 非目标 |
|---|---|
| 桌面 + 手机浏览器「打开即用」,可装 PWA | 端到端多端合并编辑(密文不可合并,冲突手动二选一) |
| 服务器零知识,库文件泄露不新增爆破面 | 防窥(分类名出现在对象键上,用户已明确不需要) |
| 支持图片附件、长文教程 | 账号体系、注册登录、分享 |
| 写入绝不静默覆盖(CAS + 冲突弹窗) | 版本历史长存(只有 `backup/` 滚动 10 份) |

## 2. 运行环境与平台支持

| 项 | 结论 |
|---|---|
| 部署 | Cloudflare Worker(静态资产由 `[assets]` 分发,`/api/*` 进 Worker)+ R2 桶;免费套餐即可 |
| 浏览器 | 任何现代浏览器(Chrome/Edge/Firefox/Safari/移动端);HTTPS 由平台提供 |
| 本地体验 | `node dev-server.mjs`(同一套 Worker 代码 + 内存 R2,数据重启即清空,仅调 UI) |
| PWA | manifest + Service Worker(只缓存应用外壳,绝不缓存 `/api/*` 数据) |

## 3. 存储布局(R2 桶内)

```
<桶>/
├── vault.json           库级参数:版本、KDF 参数、salt、被 KEK 包裹的 DEK、鉴权哈希(几乎从不改动)
├── cats/<分类名>.enc    一级分类「分类名」的全部笔记(密文)
├── blobs/<名>           图片附件,文件名 = HMAC(文件名密钥, 内容哈希) + 扩展名,内容不可变
└── backup/<分类名>/     该分类被覆盖/删除前的旧版,滚动保留 10 份
                        (文件名 = <分类名>.<13位毫秒>[-<6位随机>].enc)
```

规则:

- **一级分类 = 一个 `.enc` 对象**,分类名即对象键(去 `.enc` 后缀),保持可读。
- Worker 只读写上述前缀;分类名经严格校验(禁路径分隔符/控制字符,≤100 字符)。
- 删分类 = 删对应 `.enc`(删前先备份);孤儿图片由应用内**手动触发**的「清理未引用图片」回收。
- **孤儿清理必须 fail closed**:它是全应用唯一「不可恢复的删除」——blob 没有备份层
  (不像分类那样删前先进 `backup/`),误删一张图就是永久丢失。所以清点不全时**整次中止、
  一张不删**:①先 `rescan()` 刷新分类清单(本机那份可能是解锁时拉的,别的设备此后新建的
  分类不在其中,只按本地清单清点会把它们的图全判成孤儿)②任一个分类读不出来
  (损坏或一次瞬时 GET 失败)即抛错中止,绝不「跳过它、照删其余的」。
- 备份名的随机后缀是必需的:连续 12 次 `Date.now()` 会返回同一个值(实测),
  只靠时间戳命名会让同一毫秒内的多份备份互相覆盖。轮换时必须按原文件名删除,
  不能用 `base/ts` 重新拼(会丢后缀 → 删不掉)。

## 4. 文件格式定义

### 4.1 vault.json(明文 JSON,不含任何机密)

```json
{
  "magic": "JMBIJI",
  "version": 1,
  "kdf": { "name": "PBKDF2", "hash": "SHA-256", "iterations": 1000000, "salt": "<base64 16B>" },
  "wrap": { "alg": "AES-KW", "wrappedDek": "<base64>" },
  "verifier": { "nonce": "<base64>", "ct": "<base64>" },
  "auth": { "hash": "<sha256 hex 64>" }
}
```

- 全库**单点**:DEK 只在此被包裹。**建库后、改主密码后,必须把本文件抄送一份到库外**——唯一需要养成的人工习惯。
- 改主密码 = 用新 KEK 重包 DEK + 换 verifier + 换鉴权哈希,只重写本文件(CAS),**不重加密任何 `.enc`**。
- `auth.hash` = SHA-256(鉴权令牌),服务端 Bearer 校验用;令牌本体从不落盘。

### 4.2 分类文件 `*.enc`(二进制)

| 段 | 长度 | 内容 |
|---|---|---|
| 魔数 | 4 B | `JMB1` |
| 版本 | 1 B | `0x01` |
| nonce | 12 B | 每次保存**重新随机生成**,绝不复用 |
| 密文 | 剩余 | AES-256-GCM(内容子密钥),AAD = 前 17 字节;明文为该分类的 JSON |

约束:

- 格式内保留 `version` 字段;某分类超 20–30 MB 才引入文件内记录级分片,届时无损升级。
- **禁止在应用外复制 `.enc`**(同钥同 nonce 加密两份不同明文会破坏 GCM 保密性)。
- 分类共用同一把 DEK + 各自独立 nonce,安全(文件数少,碰撞概率可忽略)。

### 4.3 blobs/ 图片附件

- 文件名 = `HMAC(文件名子密钥, SHA-256(图片原始字节))` 的 base64url + 原扩展名(≤12 字符)。
- **内容不可变**:写一次永不再动;同图重复插入只存一份(服务端 `If-None-Match: *` 去重,已存在返回 200)。
- 显示:解密后 `URL.createObjectURL(blob)`;**禁止**把解密图片转 `data:` 拼进 HTML 字符串。

## 5. 加密方案

| 环节 | 方案 |
|---|---|
| 口令派生 | 主密码 → PBKDF2-HMAC-SHA256 **100 万次**(本机实测 336 ms,仅解锁时一次)→ KEK 裸字节 |
| 信封加密 | DEK 随机 32B,由 KEK 经 **AES-KW** 包裹存于 `vault.json`;改密码不重加密库 |
| 子密钥分离 | DEK 经 **HKDF** 派生:`内容` / `附件` / `文件名` 三把,`info` 各不相同,禁止一钥多用 |
| 鉴权令牌 | KEK 裸字节再经 HKDF(info=`JMBIJI-auth-v1`)派生;客户端以 hex 作 Bearer;服务端只存 SHA-256(令牌) |
| 认证加密 | AES-256-GCM;nonce 每次保存新生成;**AAD 绑定文件头**,头部改一字节即解不开 |
| 密钥驻留 | **默认**仅内存:锁屏/关闭时置 null(best-effort)、不写日志、不落盘。勾选「记住本设备」后 DEK + 鉴权令牌会存进 localStorage(见 §7.5,代价见 §11) |
| verifier | `vault.json` 加密校验块,区分「密码错」和「文件损坏」 |
| 未来升级 | KDF 能力探测式:浏览器支持 Argon2id(内存硬)后自动切换;现在不引 WASM 依赖 |

解锁失败提示区分:主密码错误 / 文件损坏(verifier + GCM 认证标签)。

迭代次数收口(`clampIterations`):非法值(0/负数/NaN/缺失)回落默认 100 万,
**上限 800 万** —— 上限是防「被篡改的 `vault.json` 写个天文数字把标签页冻死」,
不是性能旋钮。低于 10 万只提示(`weakKdf`)不阻断:把迭代次数改小对攻击者毫无帮助。

### 5.1 主密码策略

强度按**估算熵**(`长度 × log2(字符池)`)分档,而不是数「用了几类字符」——
后者会把 20 位纯小写(94 bit)误判为弱、却放行 10 位混合(52 bit)。

| 判据 | 规则 |
|---|---|
| 硬底 | < 10 位:直接拒,提示「太短」(优先于熵,给出更可操作的提示) |
| 直接拒 | 命中高频弱口令表 / 含明显连续串(`1234567890`、`qwertyuiop`…)/ 整串同字符 |
| 通过线 | 估算熵 ≥ **64 bit** 判「够用」,≥ **90 bit** 判「很强」 |
| 反馈 | 建库输入框实时显示 弱/中/强 与估算熵;改主密码时先校验再要求二次确认 |

刻意**不涨价迭代次数**:每翻一倍只多 1 bit,却让每次解锁多等一倍
(本机 100 万 = 336 ms、200 万 = 672 ms、400 万 = 1.3 s;手机还要再乘 2~4 倍,
而令牌只存内存 —— 刷新页面就要重来一次)。同样预算花在
「10 位以上 + 必设访问密钥」上收益高一个数量级。

## 6. 数据模型(密文内部的 JSON)

```json
{
  "notes": [
    {
      "id": "uuid",
      "title": "Cloudflare 中转",
      "content": "Markdown 子集文本",
      "order": 1000.0,
      "createdAt": 1690000000000,
      "updatedAt": 1690000000000,
      "attachments": [{ "file": "<blob文件名>", "name": "截图.png" }]
    }
  ]
}
```

- 树只有**两级**:分类(=对象)→ 笔记(flat 数组),浮点 `order` 排序(插中间取均值)。
- 标题、正文、排序键全部在密文内部,对象键只暴露分类名。
- 归一化宽容读取:字段缺失补默认值,结构完全不对才报 FormatError。

## 7. 交互设计

### 7.1 布局

- 左侧:分类列表 + 笔记列表(**懒加载**:点开分类才拉取并解密该文件)。
- 右侧:阅读/编辑区;顶栏:搜索、保存状态、锁定。

### 7.2 读为主、复制为主

| 功能 | 行为 |
|---|---|
| 阅读态优先 | 打开即阅读态;点「编辑」或双击才进入编辑态 |
| 全文复制 | 每条笔记一键复制全文 |
| 字段级复制 | 行内代码/代码块、`key: value` 行、列表项逐项提供复制入口 |
| 复制反馈 | 按钮变「已复制」约 1.5 秒 |

### 7.3 渲染(自写渲染器,零依赖)

- 支持:`#` 小标题、`**加粗**`、`==高亮==`、`` `行内代码` ``、三反引号代码块、`-` 列表。
- **不解析链接、不解析 HTML**;**一律 DOM 构建,禁止 `innerHTML`**。

### 7.4 搜索

- 顶栏实时过滤标题与正文(先解密全部已加载分类);命中高亮 + 跳转。

### 7.5 锁屏与「记住本设备」

- 启动进锁屏,输主密码解锁;解锁时重新执行 KDF 并派生鉴权令牌。
- **「记住本设备」**(锁屏页勾选,**默认勾选**):解锁成功后把 `{DEK, 鉴权令牌}` 存进本浏览器
  (`localStorage` 的 `jmbiji.session`),之后打开网址直接恢复会话,**不再输主密码**。
  恢复前先与 `vault.json` 的校验块比对(本机存的 DEK 可能属于另一个库 —— 换过桶、
  被别的库覆盖);比对不过或令牌已失效(多半是别的设备改过主密码)**都会清掉会话、
  回落锁屏并说明原因**,不静默失败,也不留下「进去了但每个分类都打不开」的怪状态。
- **代价(必须写清,别让它变成「看起来加密了」的错觉)**:存的是**明文密钥**。
  任何能读到本浏览器存储的人或代码都能直接解开全部笔记,**不需要**再爆破主密码。
  服务端零知识性质不受影响:R2 里仍然只有密文,主密码仍然从不上传。
  这是「打开即用」必然的代价 —— 存 localStorage / IndexedDB / Cookie 只是形式不同,
  不存在「既免密又不必落盘密钥」的方案。
- **「锁定」= 退出登录**:清内存 + **清掉本机会话**。不清的话锁定形同虚设(刷新一下就自动进去了)。
- 自动锁屏可选,**默认关**。它与「记住本设备」目的相反(一个要「离开就得输密码」,
  一个要「打开即用」),默认给后者;开启后空闲到点会锁定并一并清掉本机会话,语义一致。

## 8. 保存与同步策略

### 8.1 触发时机

| 时机 | 动作 |
|---|---|
| 手动 | 保存按钮 / Ctrl+S |
| 兜底 | 改动停止后 **5 分钟**自动保存 |
| 切走页面 | `visibilitychange` hidden → 立即保存一次 |
| 关闭页面 | 有未保存改动 → 浏览器确认框 |

### 8.2 写入纪律(每次保存都必须执行)

1. **CAS 条件写**:PUT 带 `If-Match`(上次见到的 etag);服务器版本已变 → 412 → 弹冲突三选(**用我的版本覆盖** / 以云端为准 / 取消),绝不静默覆盖。「强制覆盖」走重读当前 etag 后再写,服务器仍先把旧版送进 `backup/`。
2. **仅新建**:`If-None-Match: *` 语义(R2 `onlyIf: { etagDoesNotMatch: '*' }`,workerd 特判 `*` 为通配符);已存在 → 409。
3. **服务端备份**:CAS 成功的覆盖、删除,服务器先把旧对象写入 `backup/<分类名>/`,滚动保留 **10 份**(轮换逻辑复用前端 `format.js` 纯函数,单一出处)。被拒绝的写入不产生备份。
4. R2 强一致:写成功后任何后续读立即可见,无最终一致窗口。

### 8.3 多端边界

- 多台设备可同时使用;同一分类的并发编辑靠 CAS 检出,冲突手动二选一。
- 不同分类并行编辑互不冲突(各一个对象、各自 CAS)。

### 8.4 多标签页同步(同一个浏览器)

「两台设备各改各的」在单人用法里几乎不出现;但**同一台电脑开两个标签页**很常见 ——
A 保存之后 B 手里就是旧数据,B 再保存必撞 412 弹窗,而「用我的版本覆盖」会把 A 刚改的
其他笔记一并盖回去(云端有备份、能恢复,但很费解)。这类冲突不该发生。

做法:`BroadcastChannel`(`public/js/tabsync.js`),三种消息 ——
`cat-saved`(某分类被保存)/ `cats-changed`(分类增删改名)/ `locked`(退出登录)。

★ 两条硬规则:

1. **消息只当「去重新核对」的提示,绝不当权威状态。** 收到 `cats-changed` 一律走 `rescan()`
   重新问服务器,不照消息改内存。于是即便消息是错的(或来自同源的恶意页面),
   最坏也只是多拉一次数据。
2. **本地有未保存改动时,绝不自动刷新** —— 那等于静默丢掉用户正在写的东西,比冲突弹窗糟得多。
   这种情况只提示「保存时会冲突」,由用户自己决定。

`locked` 必须同步的理由:锁定会清掉共享的 localStorage 会话,不通知的话另一个标签页
还开着(内存里仍有令牌与明文),「锁定」就等于没生效。

环境不支持 `BroadcastChannel` 时静默降级为「不同步」,冲突弹窗照旧兜底 —— 同步是锦上添花。

## 9. 备份与恢复

按「**是不是和原件在同一个地方**」分层 —— 这才是备份是否有用的分界线:

| 层 | 机制 | 在哪 | 内容完整度 |
|---|---|---|---|
| 第 1 层 | 服务端 `backup/` 滚动 10 份(覆盖/删除前自动) | **同一个桶** | 单分类的历史版本 |
| 第 2 层 | R2 桶版本控制(可选,控制台手动开) | **同一个桶** | 全桶任意时点 |
| 第 3 层 | `vault.json` 库外手动副本(见 §4.1) | 库外 | **只有钥匙,没有箱子** |
| 第 4 层 | **全库密文包**(侧栏「导出全库备份」) | 库外 | **完整**(钥匙 + 全部分类 + 全部附件) |

⚠️ **第 1、2 层跟着桶一起没**:桶被误删或账号出问题,它们一并消失。
第 3 层是原先唯一「库外」的东西,但它只是把钥匙 —— 桶没了,那把钥匙打不开任何东西。
**所以第 4 层才是真正意义上的备份**,这也正是补它的原因。

- **导出**:全程**不解密**,把 `vault.json` + `cats/*.enc` + `blobs/*` 打成一个 **store-only 的普通 zip**
  (`public/js/zip.js`,零依赖;密文不可压缩,压缩率≈1,压了白花 CPU)。
  做成标准 zip 的理由:人工抢救时双击就能看见里面的文件,不需要先写个解析器。
  前置提示真实体积(个人库也可能上百 MB,手机上有内存压力)。
- **恢复**:只做「仅新建 + 附件内容寻址去重」,**已存在的一律跳过、绝不覆盖**
  (目的是补回缺的,不是把服务器上较新的版本盖回旧版)。
  包里没有合法 `vault.json` → 拒绝;目标桶里是**另一个库**(包裹的 DEK 不同)→ 拒绝 ——
  把另一个库的密文塞进来只会得到一堆永远解不开的文件。
- 包内不含 `backup/` 的历史版本(本包是「当前全量」;Worker 也没有暴露 backup/ 的接口,这是有意的)。

恢复路径(由轻到重):单分类损坏 → 从 `backup/<分类名>/` 拿最近一份放回 `cats/`;
整桶丢失 → 新建桶,用全库密文包「从备份恢复」,再用原主密码解锁。

## 10. Worker 接口与实现要点

| 项 | 决定 |
|---|---|
| 架构 | 纯函数核心 `handleApi(method, url, headers, body, env)` + 薄适配层(Request↔参数);Node 离线单测全覆盖 |
| 接口 | `GET/PUT /api/vault`(读免鉴权/建库仅一次/改密码需鉴权+CAS);`GET /api/cats`;`GET/PUT/DELETE /api/cat?key=`;`GET /api/blobs`(回 `{names, blobs:[{name,size,uploaded}]}`,size 供导出前提示体积;`names` 保留给既有调用方);`GET/PUT/DELETE /api/blob?key=`;分类/附件名一律走 `?key=` 查询参数(避开路径归一化差异) |
| 鉴权 | 除 vault 读写建库外全部要求 `Authorization: Bearer <hex64>`;连续失败 15 次 → 封禁 10 分钟并回 `retry-after` |
| **跨请求状态** | 只放**模块级**:`env` 是请求级对象(CF 文档:绑定按 per-request 应用),挂在它上面的数据随请求一起丢弃。限流计数器曾因此**完全失效**而单测仍全绿(用例复用了同一个 `env`),现在有「每次请求新 env」的回归用例钉住 |
| 限流计数条件 | **只在请求带凭据时计数**。跨站页面只能发不带自定义头的简单请求(带 `Authorization`/`X-Access-Key` 会触发 CORS 预检,而本服务不返回任何 CORS 头 → 预检必败),该判据同时挡掉「借受害者 IP 刷满失败预算」这条跨站锁死路径;限流表容量上限 4096,防海量 IP 撑爆内存 |
| 访问密钥门 | `env.ACCESS_KEY` 所有 `/api/*` 共用,须为 ASCII 且 **≥16 字符**;**未设置或过短一律 fail closed(503)**,不静默降级(它是唯一挡住「拿到网址者拉走 vault.json 离线爆破」的一层)。显式放弃:`ALLOW_NO_ACCESS_KEY=1`(强烈不推荐) |
| 比较 | 令牌哈希与访问密钥都走定长比较;访问密钥先两侧各取 SHA-256 再比定长摘要,既不泄露长度也不短路 |
| 跨站 | `OPTIONS` 显式一律 405,永不返回任何 CORS 头(`_headers` 里也不加) |
| 硬化响应头 | 所有 API 响应带 `nosniff` / `no-store` / `Referrer-Policy` / `X-Frame-Options` / `default-src 'none'` 的 CSP;静态资源由 `public/_headers` 提供(含完整 CSP、HSTS、`X-Robots-Tag: noindex`)。注意 `_headers` **不作用于 Worker 生成的响应**,两处都要有 |
| 读 body 前预检 | 适配层先看 `content-length`,超过 50MB+1MB 直接 413,不把大 body 读进 128MB 的 isolate |
| 大小上限 | vault.json 16KB / 分类 30MB / 附件 50MB,超出 413 |
| 名称校验 | 分类名 ≤100 字符、禁 `/ \ ` 与控制字符;附件名 = 43 字符 base64url + 可选扩展名 |
| 列表翻页 | `listAll()` 按游标翻页(真机单次上限 1000 条),否则第 1001 个对象会在界面上「凭空消失」 |
| 备份轮换 | `worker/memory-r2.mjs` 为真机 R2 语义的内存复刻(条件写失败返回 null、`pageSize` 可调以逼出翻页路径),单测与 dev-server 共用 |
| 条件写陷阱 | R2 绑定**只认** `onlyIf`(R2Conditional 对象或 Headers);传不存在的选项名会被**静默忽略**=无条件写。CAS 用 `{ etagMatches: <裸etag> }`(禁止带引号),仅新建用 `{ etagDoesNotMatch: '*' }` |
| 验证纪律 | `node tests/falsify.mjs`:逐条破坏守卫并确认对应用例**变红**再还原。全绿不说明任何事,能按需变红才有判别力 |
| 桶的可见性 | README 部署步骤显式要求**私有桶**(不开 `r2.dev`、不绑自定义域、不启用 Public Access)。代码**无法自检**:绑定读不到桶的公开配置,而为了自检去配 R2 S3 凭证等于新增一条持久密钥面,不划算 → 由 GitHub 流水线用管理面 API 查(`domains/managed` 的 `enabled` 必须 false、`domains/custom` 必须为空) |
| 配置注入 | wrangler **不支持**在配置里插值环境变量,自定义域又是结构化字段 → `scripts/gen-config.mjs` 把模板 + 环境变量合成生成物。模板只放**合法默认值**(不放 `__TOKEN__` 占位符,否则 `npm run dev` 都起不来);注入按**键名逐行锚定**替换、替换值用函数式回调(防 `$` 被当替换模式);布尔量必须写裸值(`workers_dev = "false"` 在 TOML 里是字符串,wrangler 会报类型错误);生成后**回读校验**,并断言生成物中**不含 ACCESS_KEY** |
| 上线验收 | CI 部署完真的去请求线上,而不是只看 wrangler 退出码:首页 200 + `CSP`/`X-Frame-Options`/`nosniff` 齐备 + **无密钥访问 `/api/vault` 必须 401**(200 = 门形同虚设;503 = 密钥没同步)+ 带密钥必须 200/404(401 = 两边密钥不一致)。三条判据分别对应 S3/S2 与密钥同步,结果写进 Actions Summary |
| 公开仓库 | 只监听 `push(main)` 与 `workflow_dispatch`,**禁止** `pull_request*`(公开仓库下 `pull_request_target` 会把 Secrets 交给 fork 的代码执行);权限固定 `contents: read`;checkout 关凭据落盘;密钥只经 `env:` 传入、不内联进脚本正文。以上均由 `tests/deploy.test.mjs` 的文本断言钉住,并已用变异测试确认会变红 |
| 量化数字 | 报告里的速率/耗时一律由本机实测反推并写明推导链,不引用未经核对的二手基准(初稿「2 万次/秒」即因此被修正掉一个数量级) |

## 11. 已知限制(界面与文档需明示)

1. 主密码忘记 = 数据不可恢复,无任何后门。
2. **R2 桶必须保持私有**(不开 `r2.dev` 公开域、不绑自定义域、不启用 Public Access)。
   本项目所有读写都走 Worker(`env.VAULT` 是绑定),桶从不需要对外可达;一旦公开,
   全部密文可直接下载,且 `ACCESS_KEY` 门(只拦 `/api/*`)**整体失效**、限流也不再约束攻击者。
   ⚠️ 与 r2share(下载走公开桶直链)的配置要求**相反**,同机操作时最容易搞混。
3. 服务器只有滚动 10 份备份,更早历史不保留(可选开 R2 桶版本控制兜底)。
4. 密文不可合并:同一分类并发编辑后保存方手动二选一。
5. 分类名暴露在对象键上(有意为之,便于人工辨认与抢救)。**这是本设计最大的元数据泄露面**:
   R2 桶的可读方能看到「秘钥」「攻略」这类分类名与各文件大小,只是看不到内容。
   审计结论为**不修**:改键名只防「能读不能写」的窄窗口,挡不住能改代码者(那是云端零知识方案的
   共同上限),却会带来迁移、逐文件解密与丧失控制台抢救便利。零成本替代:分类名用中性串。
   若将来要改,范围见 `AUDIT.md` 文末。
6. 浏览器崩溃且恰在保存间隔内,最多丢本次未保存的编辑(5 分钟兜底已压到最低)。
7. 限流按 **isolate** 计:冷启动、换代、多机房各算一份,是尽力而为而非硬保证。
   要更硬的限流需接 Workers Rate Limiting 绑定或 Durable Object(都会引入配置/依赖,暂不引入)。
8. 定长比较的**时序性质无法用功能测试判别** —— 任何功能等价写法都得到相同的通过/拒绝结果,
   它只能靠代码审阅保证(`tests/falsify.mjs` 里刻意不列这个变异体,免得造成「已验证」的错觉)。
9. `vault.json` 的库外副本(§9 第 2 层)本身就是离线爆破的目标:拿到它的人可以不受
   访问密钥门与限流的任何约束,离线穷举主密码。**所以第 2 层备份不要放在会外泄的位置**
   (浏览器下载目录、公共网盘),而主密码强度与迭代次数正是为这一刻准备的。
10. **默认**不持久化:解锁令牌只存内存,刷新页面/关标签页即失效,需重新输主密码
    (本地要跑一次 100 万次 PBKDF2)。勾选「记住本设备」可免除这一步,代价见 §7.5 与第 16 条。
11. 仓库公开 ≠ 站点入口公开,但**入口地址不再是秘密**:README/截图/公告里一旦出现真实网址,
    它就只剩「访问密钥 + 主密码」两道锁。零知识保证的是**内容**不被读到,不是「没人知道有这个站」。
12. 用 GitHub 流水线部署时,**仓库里的 Secrets 是权威来源**:每次部署都会覆盖 Worker 上同名 secret。
    想临时改密钥请改仓库设置后重跑,别只改 Cloudflare 控制台(下次 push 会被覆盖回去)。
13. CI 的桶可见性审计对「查不到」只告警不失败(见 §10):若 API Token 缺 R2 只读权限,
    这一步会打印「未能判定」——请自行到控制台确认 r2.dev 处于 Disabled 且没有绑自定义域。
   这是刻意取舍 —— 查不到与「确认没开」必须区分开,但不该为此拦下一个合法部署。
14. **孤儿清理有秒级残余窗口**:`rescan()` 到逐个删除之间,别的设备若恰好新建分类并附图,
   那张图仍可能被误删。要更硬的保证需在服务端做标记-清扫,或给新 blob 设冷却期
   (但冷却期要用服务端时间判定,否则客户端时钟偏移会让它形同虚设)。暂不引入。
15. **blobs 没有备份层**:分类有 `backup/` 滚动 10 份,图片没有 —— `DELETE /api/blob`
   直接删对象。所以「清理未引用图片」误删不可逆,这也是它做成 fail closed 的原因。
16. **「记住本设备」把 DEK 与令牌写进 localStorage**:能读该存储的人或代码即可直接解密全部笔记,
   且完全绕过主密码强度与迭代次数这两道防线。这是为「打开即用」付的价 ——
   默认开启,因为本项目的主要场景是「自己的电脑、自己一个人用」;换公用电脑请取消勾选。
   注:它**不**降低服务端的安全性(R2 里仍只有密文),降低的是「本机被别人拿到」时的门槛。
17. **SW 的缓存版本号必须手动升**:`public/sw.js` 是 cache-first,浏览器只在 `sw.js` 本身
   变化时才重新安装它 —— 改了 `public/` 下的资源却不升 `CACHE` 版本号,老用户会永远停在旧代码上
   (表现为「部署了但界面没变」)。新增 js 模块还要同步登记进 `ASSETS`,漏了会离线白屏;
   后者已由 `tests/assets.test.mjs` 自动核对,**前者无法自动断言**,只能靠这条约定。
18. **全库备份包是离线靶子,而且比 `vault.json` 副本更该收好**:它含全部密文,
   拿到它的人不受访问密钥门与限流保护,可以离线穷举主密码。零知识保证了「内容读不到」,
   但**主密码强度就是那一刻的全部防线**(§5.1 的 ≥10 位 / ≥64 bit 正是为它准备的)。
19. **导出要在内存里组装整个包**:当前实现把全部密文读进内存再打包,峰值约为库大小的两倍。
   个人库无妨;上百 MB 建议在桌面端做。要根治得改成流式打包(Worker 侧无此问题,
   因为它是逐个对象读写的)。
20. **多标签页同步按浏览器生效**,跨设备不适用(跨设备本来就靠 CAS 冲突弹窗兜底)。
   环境不支持 `BroadcastChannel` 时静默降级,不影响其余功能。

## 12. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M1–M4(本地版) | 基础闭环 / 阅读体验 / 附件 / PWA 打磨 | ✅ 已完成 |
| C1 云端化 | Worker + R2 后端、Bearer 鉴权、CAS 冲突、服务端备份、dev-server、单测 | ✅ 已完成 |
| C2 部署 | wrangler deploy + ACCESS_KEY + 手机端实测 | 待用户执行 |
| C3 安全审计 | 全站审计:限流失效修复、访问密钥门 fail closed、CSP 与硬化头、定长比较、翻页、备份命名碰撞、变异测试 | ✅ 已完成 |
